App\Entity\Onboarding\ServiceAccount:
  #security: 'is_granted("PERMISSION_API_ONBOARDING")'
  attributes:
    pagination_items_per_page: 25
  ##description: 'Kommunalportal Servicekonto NRW'

  operations:
    ApiPlatform\Metadata\GetCollection:
      normalizationContext:
        enable_max_depth: 1
        groups: ['collection:read', 'service_account:collection:read']
      openapiContext:
        description: 'Gibt eine Liste mit Kommunalportal Servicekonto NRW zurück'

    ApiPlatform\Metadata\Get:
      normalizationContext:
        enable_max_depth: 1
        groups: ['item:read', 'service_account:read']
      openapiContext:
        description: 'Gibt einen Eintrag der Kommunalportal Servicekonto NRW zurück'