App\Entity\Onboarding\XtaServer:
  #security: 'is_granted("PERMISSION_API_ONBOARDING")'
  attributes:
    pagination_items_per_page: 25
  ##description: 'Bauportal.NRW Untere Bauaufsichtsbehörde'

  operations:
    ApiPlatform\Metadata\GetCollection:
      normalizationContext:
        enable_max_depth: 1
        groups: ['collection:read', 'xta_server:collection:read']
      openapiContext:
        description: 'Gibt eine Liste mit Einträgen für Bauportal.NRW Untere Bauaufsichtsbehörde zurück'

    ApiPlatform\Metadata\Get:
      normalizationContext:
        enable_max_depth: 1
        groups: ['item:read', 'xta_server:read']
      openapiContext:
        description: 'Gibt einen Eintrag für Bauportal.NRW Untere Bauaufsichtsbehörde zurück'